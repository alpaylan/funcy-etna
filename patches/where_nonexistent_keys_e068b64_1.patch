diff --git a/funcy/colls.py b/funcy/colls.py
index 4c466fb..791b67c 100644
--- a/funcy/colls.py
+++ b/funcy/colls.py
@@ -357,7 +357,7 @@ def linvoke(objects, name, *args, **kwargs):
 def where(mappings, **cond):
     """Iterates over mappings containing all pairs in cond."""
     items = cond.items()
-    match = lambda m: all(k in m and m[k] == v for k, v in items)
+    match = lambda m: all(m[k] == v for k, v in items)
     return filter(match, mappings)
 
 def pluck(key, mappings):
